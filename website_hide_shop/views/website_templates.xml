<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="layout" inherit_id="website.layout" name="Main layout inherited">
        <xpath expr="//ul[@id='top_menu']/t[@t-foreach='website.menu_id.child_id']" position="replace">
            <t t-foreach="website.menu_id.get_active_menus()" t-as="submenu">
                <t t-call="website.submenu"/>
            </t>
        </xpath>
    </template>

    <template id="submenu" inherit_id="website.submenu" name="Submenu inherited">
        <xpath expr="li[@t-if='submenu.child_id']" position="replace">
            <li t-if="submenu.get_active_menus()" t-attf-class="dropdown #{
                (submenu.url and submenu.url != '/' and any([request.httprequest.path == child.url.replace('/page/website.', '/page/') for child in submenu.child_id if child.url]) or
                 (submenu.url and request.httprequest.path == submenu.url.replace('/page/website.', '/page/'))) and 'active'
                }">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <span t-field="submenu.name"/> <span class="caret" t-ignore="true"></span>
                </a>
                <ul class="dropdown-menu" role="menu">
                    <t t-foreach="submenu.get_active_menus()" t-as="submenu">
                        <t t-call="website.submenu"/>
                    </t>
                </ul>
            </li>
        </xpath>
    </template>
</odoo>